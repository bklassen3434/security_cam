# Security Cam (MVP chunk 1)

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

## Run
```bash
python -m app.main
```

## Press q to quit the preview window.